{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "require_schema.json",
  "title": "Require Schema",
  "description": "Schema for the 'require' property.",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "enum": ["item"]
    },
    "item": {
      "type": "string"
    }
  },
  "required": ["type", "item"]
}
